import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProductsScreen = () => {
	return (
		<View>
			<Text>ProductsScreen sf sss</Text>
		</View>
	);
};

export default ProductsScreen;

const styles = StyleSheet.create({});
